import 'package:flutter/material.dart';

class PagosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gestión de Pagos')),
      body: Center(child: Text('Aquí va la gestión de pagos')),
    );
  }
}
