import 'package:flutter/material.dart';

class PaquetesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gestión de Paquetes')),
      body: Center(child: Text('Aquí va la gestión de paquetes turísticos')),
    );
  }
}
